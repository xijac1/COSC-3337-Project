# Python 3.13.1+ required
# All packages below are tested with Python 3.13.1

pandas
numpy
scikit-learn
networkx
python-louvain
matplotlib
seaborn
pyarrow
fastparquet
ijson
dask[complete]
xgboost
nltk
spacy
jupyter
tqdm

# GPU Acceleration (Optional)
# For XGBoost GPU support, ensure CUDA toolkit is installed
# For RAPIDS GPU support (cuDF, cuML), install via conda:
#   conda install -c rapidsai -c conda-forge -c nvidia rapids python=3.11 cudatoolkit=11.8
# Note: RAPIDS may not be compatible with Python 3.13, use Python 3.11 for full GPU support
